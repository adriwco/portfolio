import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import RoutesPages from './config/RoutesPages.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutesPages />
  </React.StrictMode>,
);
